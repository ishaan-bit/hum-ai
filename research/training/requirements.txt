# Signal-Lab neural training harness — CPU baseline (guaranteed to install/run).
# Install (Python 3.10):
#   python -m pip install -r research/training/requirements.txt --index-url https://download.pytorch.org/whl/cpu
#
# Intel Arc / XPU acceleration (optional, auto-detected at runtime via torch.xpu):
#   the CPU wheel below has NO XPU runtime. To enable the Intel GPU, install the
#   XPU build INSTEAD (needs current Intel GPU drivers; oneAPI is bundled in the wheel):
#     python -m pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/xpu
#   then re-run any command with --device xpu (or leave --device auto). The harness
#   falls back to CPU automatically if XPU is unavailable; nothing else changes.
torch==2.5.1
numpy>=1.26
# pytest is only needed for the harness tests (research/training/tests):
#   python -m pip install pytest
